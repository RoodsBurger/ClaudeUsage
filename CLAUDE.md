# CLAUDE.md

The full contributor and agent guide for this project lives in [`AGENTS.md`](AGENTS.md): repo layout, architecture and data flow, the hard-won SwiftUI rules, and the build / test / release commands.

The import below loads it so Claude Code reads the same instructions every other agent gets from `AGENTS.md`.

@AGENTS.md
